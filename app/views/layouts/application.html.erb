<!DOCTYPE html>
<html>
  <head>
    <title>QuizStudy</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- ▼ ここからヘッダー部分 -->
    <header style="padding: 10px; background-color: #f0f0f0; display: flex; justify-content: space-between; align-items: center;">
      <%= link_to '🏠 トップページに戻る', root_path, style: "font-weight: bold; color: #3366cc; text-decoration: none;" %>

      <% if user_signed_in? %>
        <p style="margin: 0;">ログイン中：<%= current_user.email %></p>
      <% else %>
        <p style="margin: 0;">ログインしていません</p>
      <% end %>
    </header>
    <!-- ▲ ヘッダーここまで -->

    <%= yield %>
  </body>
</html>